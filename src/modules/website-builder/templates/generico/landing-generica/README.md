# Landing generica

Categoria: **generico**. Piano minimo: **starter**.

Landing universale con hero, vantaggi, FAQ e CTA.

Per duplicare questo template: copia l’intera cartella, cambia `id` e `slug` in `template.json`, poi riavvia il server. Non serve modificare il codice dell’editor.
